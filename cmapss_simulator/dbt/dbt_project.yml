name: 'cmapss_go_sim'
version: '1.0.0'
config-version: 2

profile: 'cmapss_go_sim'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  cmapss_go_sim:
    staging:
      +materialized: view
    marts:
      +materialized: table
